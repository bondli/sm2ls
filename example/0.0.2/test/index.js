define(function(require, exports, module) {
    // 通过 require 引入依赖
    var app = {

        /**
         * 启动程序
         * @return {[type]} [description]
         */
        init : function () {
            console.log('page inited, current version 0.0.2');
        }

    };

    app.init();

});